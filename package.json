{
  "name": "realtime-chat-app",
  "private": true,
  "workspaces": ["shared", "server", "mobile"],
  "scripts": {
    "install:all": "npm install",
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run start --workspace mobile\"",
    "dev:server": "npm run dev --workspace server",
    "dev:mobile": "npm run start --workspace mobile",
    "lint": "eslint . && npm run typecheck",
    "typecheck": "npm run typecheck --workspace shared && npm run typecheck --workspace server && npm run typecheck --workspace mobile",
    "test": "npm run test --workspace server && npm run test --workspace mobile",
    "build": "npm run build --workspace shared && npm run build --workspace server",
    "mobile:validate": "npm run validate --workspace mobile"
  },
  "devDependencies": {
    "@eslint/js": "^9.35.0",
    "concurrently": "^9.2.1",
    "eslint": "^9.35.0",
    "prettier": "^3.6.2",
    "typescript-eslint": "^8.42.0"
  }
}
