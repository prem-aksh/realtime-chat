# Testing

Run `npm test` for backend and mobile tests, `npm run lint` for static checks, `npm run typecheck` for TypeScript, and `npm run build` for the shared and backend builds. Backend integration tests use an isolated MongoDB Memory Server.
